{
  "_comment": "Endpoint audit config. The client is GATED against the agent-server only: any client call not on the agent-server fails the gate. Additional `classify` specs are fetched (live, with a committed fallback) purely to label each gated call with the backend that actually serves it (cloud) vs no known backend at all. See scripts/endpoint-audit.mjs.",
  "specs": [
    {
      "name": "agent-server",
      "role": "gate",
      "url": "http://localhost:8010/openapi.json",
      "file": "specs/agent-server.openapi.json"
    },
    {
      "name": "cloud",
      "role": "classify",
      "url": "https://app.all-hands.dev/openapi.json",
      "file": "specs/cloud.openapi.json"
    }
  ],
  "clientGlobs": ["src/client", "src/conversation", "src/workspace"],

  "ignoreServerOnly": ["/v1/"],

  "gate": {
    "mismatch": true,
    "missingApi": false
  }
}
